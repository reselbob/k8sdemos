const chai = require('chai');
const expect = require('chai').expect;
const describe = require('mocha').describe;
const it = require('mocha').it;

describe('Producer Consumer Tests: ', () => {
    it('Can work with consumer', function(done){
        console.log("I am running: Can work with consumer.")
        done();
    });
});